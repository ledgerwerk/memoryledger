- Preserve rejection of empty content, secret-like values, unresolved placeholders, huge transcript-like content, invalid enum values, invalid scope paths, generated output without the marker, generated output containing raw `run.html`, and manual file overwrites during export.
