# Python API

The CLI is Memoryledger's primary stable interface. The pages below document
supported or intentionally semi-public Python modules; implementation modules
are labeled as internal and may change without a CLI compatibility promise.

```{toctree}
:maxdepth: 1

models
configuration
project
storage
rendering
workflows
migrations
cli-contracts
internals
```
